workspace = { members = [ "action", "data", "db","rottie_inference", "rottie_training"] }


[package]
name = "rottie"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
